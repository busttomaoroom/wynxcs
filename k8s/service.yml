apiVersion: v1
kind: Service
metadata:
  name: mnbhjg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mnbhjg"
    port: 8080
  selector:
    app: mnbhjg
